{
  "name": "redis-mcp-server",
  "version": "1.0.0",
  "description": "Redis configurations and examples for MCP servers",
  "scripts": {
    "start:adapt": "node examples/adapt_mcp_dev_redis_example.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Echo, Head of MemCommsOps",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "dotenv": "^16.0.3",
    "ioredis": "^5.3.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
